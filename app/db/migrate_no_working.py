# from app.db.models import *
#
# MIGRATIONS_DIRictory = join(HOME_DIR, "db", "migrations")
# db.migrate(command='make',
#         migration_dir=MIGRATIONS_DIRictory,
#            provider=cfg.get("db", "type"),
#            filename=join(HOME_DIR, "db", "tests", "test_" + cfg.get('db', "name")))